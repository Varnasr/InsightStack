
# Data Validation Folder

This folder includes cross-tool scripts to validate field data across:

- Python (`validate_data.py`)
- R (`validate_data.R`)
- Stata (`validate_data.do`)

Each script checks:
- Missing values
- Duplicate IDs
- Invalid gender codes

Sample data is provided in `sample_data/sample_data.csv`.
