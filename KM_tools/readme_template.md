# [Project code]: [one-line description]

*Copy this file to the root of a new project folder and fill it in. Delete the
italic instructions. Keep it under two pages; it is the page a new team member
reads first.*

## What this is

*Two or three sentences. What the programme or study does, for whom, where,
and over what period.*

## People

| Role | Name | Contact |
|---|---|---|
| Programme lead | | |
| MEL lead | | |
| Data manager | | |
| Field coordinator | | |

## Where things are

*The things that are not obvious from the folder structure
(`folder_structure_template.md`).*

- Raw data: `03_data/raw/`, received on [date] from [source]
- The cleaning script: `05_code/01_clean.py`; run it with `[command]`
- The current questionnaire: `02_instruments/[file]`, version [n]
- The MEL framework: `01_design/[file]`
- Approvals: `00_admin/[file]`

## Codes and conventions

- Project code: `[code]`, used as the prefix in every file name
  (`file_naming_conventions.md`)
- Site identifiers: *[what the id looks like, and where the list is]*
- Missing value codes: *[e.g. -999 refused, -998 do not know]*
- Rounds: *[baseline = 2024-07, endline = 2025-03]*

## How to run the analysis

*The commands, in order, that go from raw data to the tables in the report.
If there is a `run_all` script, name it.*

## Status

*What is done, what is in progress, what is next. Dated. Update when it
changes.*

Last updated: [date] by [initials]
